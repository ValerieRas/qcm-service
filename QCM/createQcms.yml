info:
  name: createQcms
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/qcms
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
