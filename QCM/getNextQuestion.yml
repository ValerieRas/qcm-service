info:
  name: getNextQuestion
  type: http

http:
  method: GET
  url: http://localhost:3001/2/question
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
