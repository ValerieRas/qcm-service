info:
  name: getNextQuestion
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/qcms/2/question
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
